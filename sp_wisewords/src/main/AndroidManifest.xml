<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.example.android.sp_wisewords">
    <!--permission to access the content provider for wisewordslib-->
    <!--<uses-permission android:name="wisewords.READ_DATABASE" />-->
    <!--<uses-permission android:name="wisewords.WRITE_DATABASE" />-->

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
    </application>

</manifest>
